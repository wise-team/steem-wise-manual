version: "3"

services:
  #§ data.config.manual.docker.services.frontend.name + ':'
  frontend:
    ports:
      #§ '- ' + data.config.manual.docker.services.frontend.port + ':' + data.config.manual.docker.services.frontend.port
      - 4000:4000
    restart: no
